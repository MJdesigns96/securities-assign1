/**
 * checkLogin function takes in a username and password and returns a boolean.
 * Returns 
 * @param {string} username 
 * @param {string}  password
 * @returns {boolean}
 */

// import { md5Encrypt } from "./md5Encrypt"

const checkLogin = (username, password) => {
    return true;
}